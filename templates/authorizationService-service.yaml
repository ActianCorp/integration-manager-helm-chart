apiVersion: v1
kind: Service
metadata:
  name: {{ template "integration-manager.authorizationService.fullname" . }}
  {{- if .Values.authorizationService.service.annotations }}
  annotations:
    {{ .Values.authorizationService.service.annotations  | toYaml | indent 4 | trim  }}
  {{- end }}
  labels:
    app: integration-manager
    component: authorizationService
    chart: {{ template "integration-manager.chart" . }}
    release: {{ .Release.Name }}
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: integration-manager
    component: authorizationService
    release: {{ .Release.Name }}
  type: "{{ .Values.authorizationService.service.type }}"
